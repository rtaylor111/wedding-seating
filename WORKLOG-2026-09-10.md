# Wedding Seating Planner - work log, 10 September 2026

Day's theme: the new venue floor plan, a genuinely offline standalone build, and a run of interaction/visual fixes - several of them corrections to bugs shipped earlier the same day. Every plan/UI change from ~16:00 on was screenshot- or real-browser-verified before committing.

Repo: `Applications/wedding-planner` (own git repo, GitHub Pages at `rtaylor111.github.io/wedding-seating`). Revert anchor `pre-offline` still stands for the offline work.

---

## New venue floor plan (replaced Option B)
- Portrait layout matching the supplied venue image: **12 guest tables** (six joined "20" pairs, horizontal double-sided - 5 seats top, 5 bottom) plus a **bridal table**.
- Fixtures repositioned: lounge pocket, two wine plinths, ceremony-flowers markers, four pillars, DJ table, wall, bar, dessert table, fire place.
- New seat orientations (`hh` horizontal double-sided, later bridal set to double-sided `v`); zoom, PDF and viewBox all updated to `0 0 600 720`.
- Mockup approved first, then the built version screenshot-checked.

### Follow-up plan corrections (from your feedback, each screenshot-verified)
- **DJ table** moved out of the dancefloor into the gap by the wall (it was intersecting the dancefloor).
- **Bridal** made double-sided (seats both edges); its label reads cleanly, clear of the dots.
- **Pillars** moved to the four corners of the dancefloor (were sitting on it).
- **Smaller seat dots** so they don't crowd the table fill.
- **Wine plinths** aligned on the right edge, labels reading inward.
- **One consistent table colour** - dropped the muddy brown "full" fill; occupancy is read from the dots alone (your call).

## Whole-plan rotate
- ↻ button by the zoom controls spins the plan in 90° steps; remembered per device.
- Geometry rotates but **labels stay readable** - lifted to an un-rotated overlay at their rotated positions (horizontal stays upright, vertical stays vertical). Verified at 90°.
- Drag-and-drop stays exact at every rotation (resolves by screen position).

## Offline capability
- **Service-worker app shell** so the live site loads with no signal.
- **Local-first**: every edit applies instantly and queues in an outbox that syncs when back online; working state + outbox persisted to `localStorage`, so a reopen (even offline) boots from the last-known plan.
- **Offline edit unlock**: a code verified good on this device before now unlocks editing with no connection.
- **Standalone file `wedding-seating-offline.html`**: single 1.14 MB file with xlsx + jspdf inlined - double-click on Mac/Windows, fully works offline (incl. exports), syncs to the shared plan when online. (Download via the repo's "Download raw file".)

## Group multi-select and move
- **Select** toggle → tap guests to build a group; selected seats show as **black dots** on the plan; a bar shows the count.
- Move by **drag-and-drop**: drag any selected guest onto a table and the whole group seats there in one undoable step (ghost/banner show "N guests"); overflow reported, not forced.
- Select mode is tap-to-toggle; a drag moves the group. Verified with a real mouse drag.

## Bug fixes (several were same-day regressions, owned)
- **Load-crash → blank plan/roster**: a filter-row restructure left `dockImports` inserting before an element that was no longer a direct child, throwing at load and killing the whole script. Fixed (anchor is the search row). *This is the one I first mis-diagnosed as a data reset and argued about - it was a real break from my own prior commit; found in one real-browser load once I actually opened the page.*
- **Phantom "someone else is moving her" conflict**: the write precondition compared a cosmetic `seatedAt` timestamp that drifts between devices, blocking moves on an unchanged guest. Server now ignores `seatedAt` in the conflict check (genuine seat clashes still blocked). Reproduced and fixed against the real `Code.gs`. **Required a script redeploy (done).**
- **Settings phantom conflict / helmet wouldn't switch**: settings precondition now echoes the server doc exactly.
- **Filter tabs crowded** on desktop: counts moved to the heading, Delete to the search row.
- Group tap-to-move flow (shipped earlier today) was broken by a select-mode early return I added in the same change - replaced entirely by the drag-and-drop above.

## Also today
- Runner drag icons (Legionary / Shieldmaiden / Spartan) behind an Icons flyout; runner is a local per-device choice.
- Read-only mode: any attempted edit opens the enter-code prompt.
- **Local device snapshots** under Versions ("on this device", cloud-proof, capped 12) - insurance against a sync wipe.

## Open / needs your device
- iPhone: PDF/Excel export saving, long-press feel, offline reconnect on a flaky signal.
- The real (non-TEST) plan is where the actual seating work happens; TEST link stays the shareable sandbox.

## Verification note
From ~16:00 I render the plan headless and screenshot it (or drive a real Chrome with synthetic input) to check my own work against your images before committing - after the blank-plan incident showed that reasoning from data instead of looking was the wrong instinct.
